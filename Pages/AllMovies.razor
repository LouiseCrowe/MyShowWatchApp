@page "/allmovies"
@using MyShowWatch.Shared
@using Microsoft.AspNetCore.Components
@inject HttpClient Http

<h1 class="page-title">All movies I'm tracking</h1>

<p>Here's a list of all your movies!</p>

@if (movies == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Show Title</th>
                <th></th>

            </tr>
        </thead>
        <tbody>
            @foreach (var show in movies)
            {
                <tr>
                    <td>@show.Title</td>
                    <td>
                        <a href="@($"showdetail/{show.Title}")" class="btn btn-primary table-btn">
                            <i class="fas fa-info-circle"></i>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}

@code {

    private Show[] movies;


    protected override async Task OnInitializedAsync()
    {
        movies = await Http.GetFromJsonAsync<Show[]>("https://localhost:5001/Show/allmovies");
    }

}
